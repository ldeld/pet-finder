<h1>Our pets:</h1>
<%= link_to "Add a pet", new_pet_path, class: "btn btn-primary" %>
<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet.id) %> - <%= pet.species %>: <%= pet.days_ago %> </li>
    <br>
  <% end %>
</ul>